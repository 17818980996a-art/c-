//内存分区模型
/*c++程序在执行时，将内存大方向划分为4个区域
1.代码区：【程序运行前】
    存放CPU执行的机器指令
    是共享的
    是只读的
2.全局区：【程序运行前】
    存放全局变量和静态变量（在普通变量前加static）以及常量
    该区域的数据在程序结束后由操作系统释放
    不在全局区：const修饰的局部变量（局部常量）
3.栈区：【程序运行后】
    由编译器自动分配释放，存放函数的参数值，局部变量等
    指针本质上也是一种局部变量
    不要返回局部变量地址
4.堆区：【程序运行后】
    由程序员分配和释放，若程序员不释放，程序结束时由操作系统回收
    在c++中主要利用new在堆区开辟内存（可以用delete释放删除）
//new的基本语法
int * func()
{
    //在堆区创建整型数据
    //new返回的是该数据类型的指针
    int * p = new int(10);
    //deleete p;释放堆区的数据
    return p;
}
void test01()
{
    int * p = func();
    cout << *p << endl;
}
int main()
{
    test01();
    return 0;
}
//结果：10（且可以反复打印出来） 
*/
//作用：赋予不同的生命周期，给我们更大的灵活编程

//new也可以开辟数组
eg： int * arr = new int[10];
//delete 释放数组
eg：delete [] arr;
