//数组：相当于一个集合，里面存放了相同类型的数据元素
/*特点;
1.数组中的每个数据元素都是相同的数据类型
2.数组是由连续的内存位置组成的
*/
/*语法：
1.数据类型 数组名[数组长度]；
eg:
int arr[3];
//给数组元素赋值
arr[0] = 10;
arr[1] = 20;
arr[3] = 30;
cout << arr[0] << endl; //结果：10
2.数据类型 数组名[数组长度] = {值1，值2 …}；
eg:
int arr2[3] = {10, 20, 30};
coout << arr2[0] << endl;//结果：10
3.数据类型 数组名[] = {值1，值2 …}；
eg:
int arr3[] = {10,20,30}
cout << arr3[0] << endl;//结果：10
*/
eg：利用循环输出数组中的元素
int arr[3] = {1,2,3};
for(int i = 0; i < 5; i++)
{
    cout << arr[i] << endl;
}//结果：
1
2
3
0
0

//一维数组名
/*作用：
1.统计整个数组在内存中的长度
eg：
int arr[3] = {10,20,30};
cout << "整个数组占用内存空间为：" << sizeof(arr) << endl;//结果：整个数组占用内存空间为：12 （int占用4个字符）
cout << "每个元素占用内存空间为：" << sizeof(arr[0]) << endl;//结果：每个元素占用内存空间为：4 （int占用4个字符）
cout << "数组中元素个数为：" << sizeof(arr) / sizeof(arr[0]) << endl;//结果：数组中元素个数为：3

2.获取数组在内存中的首地址
eg:
cout << "数组首地址为：" << arr << endl;//结果：数组首地址为：04FFA68 (为十六进制）
cout << "数组首地址为：" << (int)arr << endl;//结果：数组首地址为：199222416 （为十进制）
cout << "数组中第一个元素的地址为：" << (int)&arr[0] << endl;//结果：数组中第一个元素的地址为：1808752 (只取一个数字的话注意加&)
*/
//注意：数组名不能赋值

//冒泡排序
/*作用：
1.比较相邻的元素。如果第一个比第二个大，就交换他们两个。
2.对每一对相邻元素做同样的工作，执行完毕后，找到第一个最大值。
3.重复以上的步骤，每次比较次数-1，直到不需要比较。
*/

//二维数组：在一维数组上多加一个维度
//语法：（同样可以用for循环进行输出）
1.数据类型 数组名[行数][列数]
2.数据类型 数组名[行数][列数] = { {数据1，数据2} ， {数据3，数据4 } }//建议用这种，代码的可读性更高 
eg:
int arr2[2][3]=
{
    {1,2,3},
    {4,5,6}
};
for (int i = 0; i < 2; i++)
{
    for (int j = 0; j < 3; j++)
    {
        cout << arr2[i][j] << " "; //输出一个数字后空两个空
    }
    cout << endl; //每行输出完换行
}
/*结果：
1  2  3
4  5  6
*/
3.数据类型 数组名[行数][列数] = {数据1，数据2，数据3，数据4}
eg：
int arr3[2][3] = {1,2,3,4,5,6};
结果同上
4.数据类型 数组名[ ][列数] = {数据1，数据2，数据3，数据4}
eg:
int arr4[][3] = {1,2,3,4,5,6};
结果同上

//二维数组名称
/*作用：
1.查看二维数组所占内存空间
eg:
int arr[2][3] = 
{
    {1,2,3}
    {4,5,6}
};
cout << "二维数组占用空间内存为：" << sizeof(arr) << endl;
cout << "第一行占用空间：" << sizeof[0] << endl;
结果：
二维数组占用空间内存为：24
第一行占用空间：12
2.获取二维数组首地址
（第一行的首地址与第一个元素和首地址的一样）
eg:
cout << "二维数组的首地址为：" << arr << endl;
结果：
二维数组的首地址为：008FFBCD
*/





























